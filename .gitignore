node_modules/
dist/
*.log

# Regenerated by `npm run android:sync` (copy of index.html/app.js/vendor/
# images into a Capacitor-friendly folder, then `cap sync`) — not source of
# truth, and would otherwise duplicate ~12 MB of vendored binaries in git.
www/
android/*.keystore

android-signing/
